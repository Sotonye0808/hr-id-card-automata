# Project AI Context

Overview: HR ID Card Automata is a single-page Vite + React PWA that helps HR teams turn employee records and photos into printable ID cards. It is local-first (IndexedDB + localStorage), config-driven (JSON templates), and focused on simple batch export to PDF and DOCX with a live preview and light image editing.

---

## Quick Reference

| Field            | Value                           |
| ---------------- | ------------------------------- |
| Project Name     | HR ID Card Automata             |
| Type             | Web App (PWA)                   |
| Primary Language | TypeScript                      |
| Frontend         | React + Vite                    |
| Backend          | None (local-first)              |
| Database         | IndexedDB (optional for images) |
| Styling          | Tailwind CSS                    |
| Deployment       | Vercel / Static hosting         |

---

## Key Modules

| Module         | Location       | Purpose                                                                      |
| -------------- | -------------- | ---------------------------------------------------------------------------- |
| Components     | src/components | UI building blocks: `IDCard`, `TemplateEditor`, `DataEntry`, `ActivityBoard` |
| Config / Types | src/types.ts   | Template and user data models                                                |
| Entry          | src/App.tsx    | App shell, initial template config, routing between editor and preview       |

---

## Agent Instructions Location

All AI agent documentation lives in `ai-system/`.

Start with: `ai-system/protocols/entry-protocol.md`

---

## Active Development Focus

Build stability and template-driven data entry. Latest work made the card back render in preview and PDF/DOCX exports (data-driven off `backLayers`), made the template library **Save** button overwrite the selected template (Save As New for copies), and kept `npm run lint`/`build`/`verify` green.
